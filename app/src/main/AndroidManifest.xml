<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comprarenelmercado">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComprarEnElMercado">
        <activity android:name=".ViewBalance"></activity>
        <activity android:name=".userViews.ViewCart" />
        <activity android:name=".userViews.User_ProductSelection" />
        <activity android:name=".userViews.User_OrderManagement" />
        <activity android:name=".userViews.User_StoreSelection" />
        <activity android:name=".userViews.User_OrderLineActivity" />
        <activity android:name=".userViews.UserOrManagerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>